<%= t '.mensagem_html',
  {
  	autor: @chamado.pessoa.nome,
  	categoria: @chamado.chamado_categoria ? @chamado.chamado_categoria.nome : ' - ',
  	criado_em: l(@chamado.created_at),
  	local: @chamado.local ? @chamado.local.nome : ' - ',
  	prioridade: @chamado.chamado_prioridade ? @chamado.chamado_prioridade.nome : ' - ',
  	status: @chamado.chamado_status ? @chamado.chamado_status.nome : ' - ',
   	texto: @chamado.texto.html_safe,
   	titulo: @chamado.titulo
   } %>