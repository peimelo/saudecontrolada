<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
  	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

  	<title><%= content_for?(:title) ? yield(:title) + ' - ' + t(:nome_sistema) :
                   t(:nome_sistema) + ' - ' + t('labels.eh_gratis') %></title>

    <link rel="shortcut icon" href="<%= image_path('favicon.ico') %>">
  	<%= stylesheet_link_tag    "application" %>
    <%= csrf_meta_tags %>
    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
    <script src="https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <div class="large-12 columns">
        <%= render_crumbs format: :html_list, last_crumb_linked: false, ul_class: 'breadcrumb', last_class: 'active' %>
        <%= render 'layouts/alert' %>
        <%= yield %>
      </div>
    </div>
    <hr>
    <%= render 'layouts/footer' %>

    <%= javascript_include_tag "application" %>
    <%# GOOGLE ANALYTICS %>
    <% if Rails.env == 'production' %>
      <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
          (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
              m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

        ga('create', <%= Rails.application.secrets.google_analytics_id %>, 'auto');
        ga('send', 'pageview');
      </script>
    <% end %>
  </body>
</html>
