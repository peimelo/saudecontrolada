<%= currently_at t('activerecord.models.referencia.other') %>
<%- model_class = Referencia -%>
<%= titulo icon('wrench', t('activerecord.models.referencia.other')) %>

<%= render 'shared/botoes_incluir_e_download', controller: 'referencias', model: model_class,
           url_xls: referencias_path(format: 'xls'), url_pdf: nil %>

<%= render 'shared/campo_buscar', model_class: model_class, url: referencias_path %>

<%= page_entries_info @referencias, model: model_class %>

<% if not @referencias.empty? %>
  <div class="table-responsive">
    <table class="table table-bordered table-condensed table-hover table-striped">
      <thead>
        <tr>
          <th><%= t('bootstrap.actions') %></th>
          <th><%= sortable model_class, 'nome' %></th>
        </tr>
      </thead>
      <tbody>
        <% @referencias.each do |registro| %>
          <tr>
            <td width="100">
              <%= botao_alterar registro, edit_referencia_path(registro), model_class %>
              <%= botao_excluir registro, model_class %>
            </td>
            <td><%= registro.nome %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% end %>
<%= will_paginate %>